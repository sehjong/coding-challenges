// PREP

// Your task is to remove all duplicate words from a string, leaving only single (first) words entries.

// Example:

// Input:

// 'alpha beta beta gamma gamma gamma delta alpha beta beta gamma gamma gamma delta'

// Output:

// 'alpha beta gamma delta'

// P: a string of words
// R: output a string with all duplicate words from the input string removed,
//    leaving only single (first) words entries
// E: Input:
//    'alpha beta beta gamma gamma gamma delta alpha beta beta gamma gamma gamma delta'
//    Output:
//    'alpha beta gamma delta'
// P:

// create a function expression that takes in a string of words
const removeDuplicateWords = str => {
    
}