// PREP

// Create a function to lowercase all strings in an array. Non-string items should remain unchanged.

// Example
// arrayLowerCase(['Red', 'Green']) == ['red', 'green']
// arrayLowerCase([1, 'Green']) == [1, 'green']

// P: an array of strings
// R: that array with all strings lowercased,
//    and non-strings unchanged
// E: arrayLowerCase(['Red', 'Green']) == ['red', 'green']
//    arrayLowerCase([1, 'Green']) == [1, 'green']
// P:

// create a function that takes in an array of strings
function arrayLowerCase(arr) {
    
}