// PREP

// Write a function that returns a sequence (index begins with 1) of all the even characters from a string. If the string is smaller than two characters or longer than 100 characters, the function should return "invalid string".

// For example:

// "abcdefghijklm" --> ["b", "d", "f", "h", "j", "l"]
// "a"             --> "invalid string"

// P: an array of strings
// R: all the even characters from a string from the array, index begins with 1
//    if the string is smaller than two characters or longer than 100 characters,
//    the function should return "invalid string"
// E: "abcdefghijklm" --> ["b", "d", "f", "h", "j", "l"]
//    "a"             --> "invalid string"
// P:

// create a function that takes in an array of strings
function evenChars(strArr) {

}