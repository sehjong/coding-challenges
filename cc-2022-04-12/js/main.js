// PREP

// Write a function which converts the input string to uppercase.

// P: a string with letters that are either lowercase, uppercase, or both lowercase and uppercase
// R: the string to all uppercase letters
// E: 'hello' => 'HELLO'
//    'AND' => 'AND'
//    'gOodByE' => 'GOODBYE'
// P:

// create a function that takes in a string of letters
function makeUpperCase(str) {
// use the toUpperCase() method on the string
    str.toUpperCase();
}